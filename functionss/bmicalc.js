function bmi(weight, height) {
    let Bmi= weight/(height*height);
    return Bmi
}

console.log(bmi(61,2).toFixed(2)); // to fixed is used for to oonly use the number which you want after the points